%h2 Edit Profile

= form_for(resource, as: resource_name, url: registration_path(resource_name), method: :put) do |f|
  - if resource.errors.any?
    #error_explanation
      %h2= "#{pluralize(resource.errors.count, "error")} prohibited this user from being saved:"
      %ul
        - resource.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :username
    = f.text_field :username

  .field
    = f.label :email
    = f.email_field :email

  .field
    = f.label :password
    = f.password_field :password, autocomplete: "new-password"
    %em Leave blank if you don't want to change it

  .field
    = f.label :password_confirmation
    = f.password_field :password_confirmation, autocomplete: "new-password"

  .field
    = f.label :current_password
    = f.password_field :current_password, autocomplete: "current-password"
    %em We need your current password to confirm your changes

  .field
  = f.label :role
  = f.select :role, options_for_select([['Player', 'player'], ['Admin', 'admin']], selected: resource.role), class: 'form-control'


  .actions
    = f.submit "Update"

%h3
  = link_to "Back", root_path

%hr/

%h3 Cancel my account
%p Unhappy? Let us know if there's anything we can do to improve your experience.
= button_to "Cancel my account", registration_path(resource_name), method: :delete, data: { confirm: "Are you sure?" }
